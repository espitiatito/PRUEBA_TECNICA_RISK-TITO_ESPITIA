# Bandeja de cobros fallidos

Herramienta interna para que Operaciones revise los cobros que fallaron en el proceso nocturno
y los reintente a mano.

Backend en `api/` (.NET 9 + SQLite), front en `web/` (Angular).
